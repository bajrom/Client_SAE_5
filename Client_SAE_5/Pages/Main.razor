@page "/"
<h3>DataCare</h3>

<div class="hero-section">
    <div class="text-container">
        <h1>Votre Portail de Visualisation des Données</h1>
        <p>
            Simplifiez la gestion de vos données de capteurs grâce à notre interface intuitive. Visualisez vos informations de manière claire et concise, identifiez les tendances et les anomalies, et améliorez la performance de vos systèmes.
        </p>
        <button class="start-button"><NavLink class="nav-link d-flex align-items-center" href="crud">Démarrer</NavLink></button>
    </div>
    <div class="image-container">
        <img id="laptop" src="/images/human_main.png" alt="Femme avec un ordinateur portable" />
    </div>
</div>

<div class="footer">
    <span>Service 100% français</span>
</div>

<style>
    body {
        background: url('/images/background.jpg') no-repeat center center fixed;
        background-size: cover; /* Augmente la taille de l'image */
    }

    .hero-section {
        display: flex;
        flex-direction: row;
        align-items: center;
        justify-content: space-between;
        padding: 20px;
    }

    .text-container {
        flex: 1;
        margin-right: 20px;
    }

    .text-container h1 {
        font-size: 2.5rem;
        font-weight: bold;
    }

    .text-container p {
        font-size: 1.2rem;
        margin: 20px 0;
    }

    .start-button {
        background-color: #6200ee;
        color: #fff;
        padding: 10px 20px;
        border: none;
        border-radius: 5px;
        cursor: pointer;
        font-size: 1rem;
    }

    .start-button:hover {
        background-color: #5300d1;
    }

    .image-container {
        position: relative;
        flex: 1;
        display: flex;
        flex-direction: column;
        align-items: flex-start;
    }

    .image-container img {
        max-width: 100%;
    }

    .footer {
        position: fixed;
        bottom: 10px;
        left: 10px;
        background: url('/images/france.png') no-repeat center center;
        background-size: cover; /* Ajuste l'image pour couvrir toute la div */
        width: 200px; /* Prend toute la largeur */
        height: 20px; /* Ajuste la hauteur selon vos besoins */
        display: flex; /* Pour centrer le contenu */
        align-items: center; /* Centrage vertical du contenu */
        justify-content: center; /* Centrage horizontal du contenu */
        color: black; /* Texte en blanc (ajustable) */
        font-size: 1em; /* Taille de la police ajustable */
    }

    #background{
    }

    #laptop{
        z-index: 50;
    }
</style>
